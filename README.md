# Visualization Plane for Hypergraph

## Development
To start the server, run:
```sh
npx --yes live-server --port=8080 --open=./index.html
```

## Testing Mobile
Using the ngrok tunnel allows for testing on mobile devices.

To start ngrok, run:
```sh
ngrok http 8080
```